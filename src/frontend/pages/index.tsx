import { Footer, Header, Main } from "@components";
import { FC } from "react";

const Home: FC = () => {
    return (
        <>
            <Header />
            <Main />
            <Footer />
        </>
    );
};

export default Home;
